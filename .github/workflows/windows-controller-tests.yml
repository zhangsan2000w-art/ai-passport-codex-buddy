name: Windows controller tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install controller dependencies
        run: python -m pip install -r tools/windows_buddy_controller/requirements.txt
      - name: Run controller tests
        working-directory: tools
        run: python -m unittest discover -s windows_buddy_controller/tests -v
